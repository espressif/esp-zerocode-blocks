id: contact-sensor
name: Door/Window Contact Sensor
description: Reed-switch contact sensor for doors and windows
keywords: [contact, door, window, reed, magnet, open, closed]
frameworks: [matter]

instances:
  - block: drivers/gpio_input_sensor
    prefix: REED
    cfg:
      gpio: 4
      pull: pullup
      active_level: 0   # reed switch typically pulls to GND when closed
      poll_interval_ms: 200
      target_param: APP_DRIVER_PARAM_CONTACT

  - block: device_types/contact_sensor
    prefix: CONTACT
    cfg:
      state_param: APP_DRIVER_PARAM_CONTACT

  - include: baseline-connectivity

  # ── Status LED + identify + commissioning indicator ────────────
  # Visible feedback during commissioning and for Matter Identify commands.
  # GPIO 15 is a sensible default (free across the catalog); reassign per
  # board as actual hardware is selected. Status-LED blocks share
  # APP_DRIVER_PARAM_LED_PATTERN so blink_pattern arbitrates between
  # commissioning_led and identify_blink.
  - block: drivers/status_led
    prefix: STATUS_LED
    cfg:
      gpio: 10
      active_level: 1

  - block: behaviors/blink_pattern
    prefix: STATUS
    cfg:
      gpio: 10
      active_level: 1
      pattern_param: APP_DRIVER_PARAM_LED_PATTERN

  - block: behaviors/commissioning_led
    prefix: COMM_LED
    cfg:
      pattern_param: APP_DRIVER_PARAM_LED_PATTERN

  - block: behaviors/identify_blink
    prefix: ID_BLINK
    cfg:
      pattern_param: APP_DRIVER_PARAM_LED_PATTERN
      identify_pattern: 3
      idle_pattern: 1

  - include: console-diagnostics

  - include: diagnostics-core

product_metadata:
  vendor_name: ZeroCode AI
  product_name: ZeroCode Contact Sensor
