id: smart-plug-metered
name: Smart Plug with Energy Metering
description: Smart plug with relay and an Electrical Sensor endpoint for power readings
keywords: [smart plug, energy, metering, power, watt]
frameworks: [matter]

instances:
  - block: drivers/relay
    prefix: APP
    cfg:
      gpio: 7
      active_level: 1
      param_id: APP_DRIVER_PARAM_POWER

  - block: drivers/button
    prefix: APP
    cfg:
      gpio: 9
      active_level: 0
      target_param: APP_DRIVER_PARAM_POWER

  # Active-power is published by a stub. Replace with HLW8012 / CT-clamp / shunt driver
  # that calls app_driver_set_param(APP_DRIVER_PARAM_ACTIVE_POWER, ...) with real readings.
  - block: drivers/electrical_meter_stub
    prefix: METER_DRV
    cfg:
      poll_interval_ms: 5000
      initial_power_mw: 0
      power_param: APP_DRIVER_PARAM_ACTIVE_POWER

  - block: device_types/smart_plug
    prefix: PLUG
    cfg:
      power_param: APP_DRIVER_PARAM_POWER

  - block: device_types/electrical_sensor
    prefix: METER
    cfg:
      active_power_param: APP_DRIVER_PARAM_ACTIVE_POWER

  - include: baseline-connectivity

  # ── Status LED + identify + commissioning indicator ────────────
  # Visible feedback during commissioning and for Matter Identify commands.
  # GPIO 15 is a sensible default (free across the catalog); reassign per
  # board as actual hardware is selected. Status-LED blocks share
  # APP_DRIVER_PARAM_LED_PATTERN so blink_pattern arbitrates between
  # commissioning_led and identify_blink.
  - block: drivers/status_led
    prefix: STATUS_LED
    cfg:
      gpio: 10
      active_level: 1

  - block: behaviors/blink_pattern
    prefix: STATUS
    cfg:
      gpio: 10
      active_level: 1
      pattern_param: APP_DRIVER_PARAM_LED_PATTERN

  - block: behaviors/commissioning_led
    prefix: COMM_LED
    cfg:
      pattern_param: APP_DRIVER_PARAM_LED_PATTERN

  - block: behaviors/identify_blink
    prefix: ID_BLINK
    cfg:
      pattern_param: APP_DRIVER_PARAM_LED_PATTERN
      identify_pattern: 3
      idle_pattern: 1

  # ── Long-press factory reset (button hold 5 s) ─────────────────
  - block: behaviors/factory_reset_long_press
    prefix: FR
    cfg:
      gpio: 9
      active_level: 0
      hold_time_ms: 5000

  # ── State persistence (survives reboot) ───────────────────────
  - block: behaviors/nvs_persist_bool
    prefix: PERSIST_POWER
    cfg:
      target_param: APP_DRIVER_PARAM_POWER
      nvs_namespace: app
      nvs_key: power

  - include: console-diagnostics

  # ── Console set/get for actuator params ───────────────────────────
  - block: behaviors/console_param_cmd
    prefix: CMD_POWER
    cfg:
      command_name: power
      target_param: APP_DRIVER_PARAM_POWER
      value_kind: bool
  - block: behaviors/console_param_get
    prefix: GET_POWER
    cfg:
      command_name: get-power
      target_param: APP_DRIVER_PARAM_POWER
      value_kind: bool

  - include: diagnostics-core

product_metadata:
  vendor_name: ZeroCode AI
  product_name: ZeroCode Metered Plug
