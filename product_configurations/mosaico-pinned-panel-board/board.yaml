schema: zc-board/1

# THE HARDWARE, NOT THE PRODUCT — see c6-devkit-board-relay/board.yaml for the
# long version. One of the catalog's SAMPLE board files: in a user's product
# this document is supplied by the host.

selected: esp-mosaico
chip: esp32s31

bmgr:
  board: esp_mosaico
  # exact: the pack defines this very board — including the CO5300 panel and
  # CST9220 touch this product also hand-pins, which is what makes it the
  # step-aside sample rather than a conflict.
  resolved_from: exact
