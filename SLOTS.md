# Slot reference

Every slot that exists across the blocks — generated by
`scripts/gen_slots_doc.py`, so this list cannot go stale. A slot is a
named C fragment (`slots/<name>.c|.h`) that the ZeroCode generator
stitches into the firmware tree at the destination below.
`{{prefix}}` / `{{prefix_lc}}` / `{{cfg.<param>}}` placeholders are
substituted per block instance.

**A slot can be taken away.** When a product runs on a board, a
block whose `bmgr:` mapping lists a slot in `replaces_slots:` does NOT
render it — esp-board-manager configures that hardware instead, so the
line has one owner rather than two. Everything else the block
contributes still renders. Write slots so that dropping the init one
means "somebody else brings this hardware up", not "the block is gone":
keep behaviour (`driver_apply_cases`, `logic_*`) out of the slot the
board manager replaces. See CLAUDE.md → Boards (`board.yaml` + `bmgr:`).

| slot | renders into | used by |
|---|---|---|
| `agents_statics` | components/app_agents (only with agents) | 2× device_types |
| `agents_tool_register` | components/app_agents (only with agents) | 2× device_types |
| `audio_command_cases` | components/app_audio (only with audio) | 29× device_types |
| `audio_commands_register` | components/app_audio (only with audio) | 29× device_types |
| `audio_event_cases` | components/app_audio (only with audio) | 1× behaviors |
| `audio_includes` | components/app_audio (only with audio) | 1× drivers |
| `audio_node_init` | components/app_audio (only with audio) | 1× drivers |
| `audio_statics` | components/app_audio (only with audio) | 1× drivers |
| `ble_hid_driver_cb_cases` | components/app_ble_hid (only with ble_hid) | 3× device_types |
| `ble_hid_statics` | components/app_ble_hid (only with ble_hid) | 2× device_types |
| `ble_mesh_driver_cb_cases` | components/app_ble_mesh (only with ble_mesh) | 4× device_types |
| `ble_mesh_level_set_cases` | components/app_ble_mesh (only with ble_mesh) | 1× device_types |
| `ble_mesh_node_init` | components/app_ble_mesh (only with ble_mesh) | 2× device_types · 1× behaviors |
| `ble_mesh_onoff_set_cases` | components/app_ble_mesh (only with ble_mesh) | 2× device_types |
| `ble_mesh_prov_cases` | components/app_ble_mesh (only with ble_mesh) | 1× behaviors |
| `ble_mesh_statics` | components/app_ble_mesh (only with ble_mesh) | 1× behaviors · 1× device_types |
| `cmake_logic_requires` | see the generator (firmware/services/src/templates/generator.ts) | 1× drivers |
| `config_defines` | components/app_config (generated header) | 59× drivers · 17× behaviors · 5× peripherals |
| `display_driver_cb_cases` | components/app_display (only with display) | 44× device_types |
| `display_includes` | components/app_display (only with display) | 5× drivers |
| `display_node_init` | components/app_display (only with display) | 5× drivers |
| `display_statics` | components/app_display (only with display) | 44× device_types |
| `display_widget_create` | components/app_display (only with display) | 44× device_types |
| `driver_apply_cases` | components/app_driver | 22× drivers |
| `driver_includes` | components/app_driver | 35× drivers · 7× peripherals |
| `driver_init` | components/app_driver | 35× drivers · 7× peripherals |
| `driver_statics` | components/app_driver | 22× drivers · 3× peripherals |
| `espnow_driver_cb_cases` | components/app_espnow (only with espnow) | 2× device_types |
| `espnow_recv_cases` | components/app_espnow (only with espnow) | 3× device_types |
| `logic_includes` | components/app_logic | 29× drivers · 23× behaviors |
| `logic_init` | components/app_logic | 29× behaviors · 29× drivers |
| `logic_statics` | components/app_logic | 29× drivers · 24× behaviors |
| `main_includes` | main/app_main.cpp | 15× frameworks |
| `main_init` | main/app_main.cpp | 15× frameworks |
| `matter_attr_cb_cases` | components/app_matter (rendered only when frameworks includes matter) | 30× device_types |
| `matter_driver_cb_cases` | components/app_matter (rendered only when frameworks includes matter) | 34× device_types |
| `matter_endpoint_create` | components/app_matter (rendered only when frameworks includes matter) | 60× device_types |
| `matter_event_cases` | components/app_matter (rendered only when frameworks includes matter) | 3× behaviors |
| `matter_identify_cases` | components/app_matter (rendered only when frameworks includes matter) | 3× behaviors |
| `matter_includes` | components/app_matter (rendered only when frameworks includes matter) | 60× device_types · 5× behaviors |
| `matter_last_fabric_cases` | components/app_matter (rendered only when frameworks includes matter) — **defaulted**: a contribution REPLACES the built-in handler (factory reset when the last fabric is removed — without some handler the device can never be commissioned again) | no blocks yet — engine default applies |
| `matter_statics` | components/app_matter (rendered only when frameworks includes matter) | 60× device_types · 1× behaviors |
| `mqtt_command_cases` | components/app_mqtt (only with mqtt) | 29× device_types |
| `mqtt_device_create` | components/app_mqtt (only with mqtt) | 44× device_types |
| `mqtt_driver_cb_cases` | components/app_mqtt (only with mqtt) | 44× device_types |
| `mqtt_event_cases` | components/app_mqtt (only with mqtt) | 1× behaviors |
| `mqtt_includes` | components/app_mqtt (only with mqtt) | 1× device_types |
| `mqtt_statics` | components/app_mqtt (only with mqtt) | 44× device_types |
| `rainmaker_device_create` | components/app_rainmaker (only with rainmaker) | 47× device_types |
| `rainmaker_driver_cb_cases` | components/app_rainmaker (only with rainmaker) | 45× device_types |
| `rainmaker_event_cases` | components/app_rainmaker (only with rainmaker) | 2× behaviors |
| `rainmaker_includes` | components/app_rainmaker (only with rainmaker) | 1× behaviors · 1× device_types |
| `rainmaker_node_init` | components/app_rainmaker (only with rainmaker) | 1× behaviors |
| `rainmaker_statics` | components/app_rainmaker (only with rainmaker) | 47× device_types |
| `rainmaker_write_cb_cases` | components/app_rainmaker (only with rainmaker) | 29× device_types |
| `sound_event_cases` | components/app_sound (only with sound) | 2× device_types |
| `vision_event_cases` | components/app_vision (only with vision) | 1× device_types |
| `vision_includes` | components/app_vision (only with vision) | 2× drivers |
| `vision_node_init` | components/app_vision (only with vision) | 2× drivers |
| `vision_statics` | components/app_vision (only with vision) | 2× drivers |
| `webui_entities` | components/app_webui (only with webui) | 12× device_types |
| `zigbee_action_cases` | components/app_zigbee (only with zigbee) | 5× device_types · 1× behaviors |
| `zigbee_attr_cb_cases` | components/app_zigbee (only with zigbee) | 23× device_types |
| `zigbee_driver_cb_cases` | components/app_zigbee (only with zigbee) | 44× device_types |
| `zigbee_endpoint_create` | components/app_zigbee (only with zigbee) | 44× device_types · 1× behaviors |
| `zigbee_includes` | components/app_zigbee (only with zigbee) | 5× device_types · 1× behaviors |
| `zigbee_node_init` | components/app_zigbee (only with zigbee) | 1× behaviors |
| `zigbee_signal_cases` | components/app_zigbee (only with zigbee) | 2× behaviors |
| `zigbee_statics` | components/app_zigbee (only with zigbee) | 44× device_types · 1× behaviors |
