description: Save/restore a uint8_t driver param across reboots via NVS (brightness, mode, etc.)

params:
  nvs_namespace: { type: string, default: app }
  nvs_key:
    type: string
    required: true

driver_params:
  target_param:
    type: u8
    required: true
