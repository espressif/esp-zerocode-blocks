#define {{prefix}}_ADC_UNIT     {{cfg.adc_unit}}
#define {{prefix}}_ADC_CHANNEL  {{cfg.adc_channel}}
#define {{prefix}}_ADC_ATTEN    {{cfg.attenuation}}
#define {{prefix}}_ADC_POLL_MS  {{cfg.poll_interval_ms}}
#define {{prefix}}_ADC_RAW_MAX  {{cfg.raw_max}}
#define {{prefix}}_ADC_SCALE    {{cfg.value_scale}}
