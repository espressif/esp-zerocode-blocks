# 8MB flash layout — for boards with 8MB flash (some esp32 / esp32s3 / esp32c6
# devkits; the 4MB variants of each are not). The matching
# CONFIG_ESPTOOLPY_FLASHSIZE_8MB ships in this block's own sdkconfig.defaults,
# so choosing the table is all a product has to do.
# OTA slots are 3MB each — comfortable headroom for full production builds with all
# behaviors (commissioning UX, OTA, console, NVS persist, recovery, etc.).
#
# Name,          Type, SubType,  Offset,    Size,     Flags
esp_secure_cert, 0x3F, ,         0xd000,    0x2000,   encrypted
nvs,             data, nvs,      0x10000,   0xC000,
nvs_keys,        data, nvs_keys, ,          0x1000,   encrypted
otadata,         data, ota,      ,          0x2000,
phy_init,        data, phy,      ,          0x1000,
ota_0,           app,  ota_0,    0x20000,   0x300000,
ota_1,           app,  ota_1,    0x320000,  0x300000,
fctry,           data, nvs,      0x620000,  0x6000,
