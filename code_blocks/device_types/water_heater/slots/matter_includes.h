#include <esp_matter.h>
/* Cluster/attribute Id constants — esp_matter.h does not include the generated *_ids.h. */
#include <thermostat_ids.h>
#include <water_heater_management_ids.h>
#include <water_heater_mode_ids.h>
